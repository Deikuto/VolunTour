# VolOnTour — frontend environment variables
# These three are PUBLIC (safe to ship in the browser). Copy to .env and fill in.
# All SECRET keys (Resend, Twilio, Turnstile secret) live in the Supabase dashboard — never here.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_TURNSTILE_SITE_KEY=
